
// Download all the changes for the public dats and relationship dats from the given user IDs
/*
module.exports = function getUpdates (user, userIDs, cb) {
  userIDs.forEach(id => {
    const pubPath = user.path + '/x' // TODO
    if (fs.existsSync(dir)) throw new Error('A dat with that name has already been created')
  })
}
*/
