const fs = require('fs-extra')
const Dat = require('dat-node')
const json = require('./utils/json')
const waterfall = require('run-waterfall')

module.exports = function makeDatPublic (user, dat, cb) {
}
